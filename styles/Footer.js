import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  footer: {
    backgroundColor:"#FFF",
    flexDirection: 'row',
    justifyContent: 'space-around',
    borderTopWidth: 1,
    borderTopColor: '#ddd',
    paddingTop: 16,
    paddingBottom:16,
  },
});
